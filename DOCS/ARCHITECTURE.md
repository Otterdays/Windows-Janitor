<!-- PRESERVATION RULE: Never delete or replace content. Append or annotate only. -->

# Architecture (index)

The full layered trust model and boundary diagrams are maintained in **[`../ARCHITECTURE.md`](../ARCHITECTURE.md)** at the repository root (historical source of truth).

This stub exists so `DOCS/` matches the expected layout; do not duplicate long-form architecture here — amend the root file when the system design changes.
